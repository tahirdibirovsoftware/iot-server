{
  "scripts": {
    "build": "rm -rf dist && npx tsc",
    "start": "rm -rf dist && npx tsc && node dist/main",
    "dev": "npx nodemon --exec npx ts-node src/main.ts"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^20.12.7",
    "nodemon": "^3.1.0",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "express": "^4.19.2",
    "serialport": "^12.0.0"
  }
}
